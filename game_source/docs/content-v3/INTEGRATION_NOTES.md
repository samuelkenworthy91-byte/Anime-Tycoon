# Integration handoff

Base: arena/01a0759f-anime-tycoon @ 2701f638f614eccce0e58a48f705918570b1f78c. Keep main untouched. These are reviewable content and art packages; no engine edits or remote changes have been made.

1. Use ASSET_ALLOCATION_MANIFEST.csv to place runtime assets, checking SHA256SUMS.txt in each pack. The runtime/ prefix inside each ZIP is packaging only; the remainder is the repository destination.
2. Review CAST_V3_RUNTIME.json as the complete 400-entry replacement content payload for the existing cast loader. Preserve the pinned original 192 fields. New portraits are under public/cast/v3. Extend the existing CastMember schema with optional epithet and display epithet ?? archetype where Arena chooses to expose it. Do not introduce a separate role or affinity system. Canonical runtime roles remain protag, secondary, pet and villain; types remain shonen/shojo.
3. Review GENRE_V3_RUNTIME.json as the full 23-genre / 253-pair content payload for the existing generated genre loader. Keep Martial Arts id martial. Samurai and Shinobi ids are samurai/shinobi and use the live scaffold ideals, ratios and RD costs. The new audience, staffing and market content is a design proposal, not evidence those hooks have been wired. Apply only fields supported by existing interfaces; document any further schema support through Arena.
4. ARC_V3_RUNTIME.json supplies add_arcs and add_combos arrays only. Append to existing data through its current schema, reject duplicate IDs, preserve the existing 60 arcs and 31 combos. Result: 84 arcs / 49 combos. Role hooks and franchise explanations in the CSV are narrative guidance, not new scoring formulas.
5. Append seven appearance pairs to WORKER_LOOKS using indexes 15–21. The occupation labels in the creation manifest describe art concepts; writer/animator/composer remain the gameplay professions. Existing hiring may independently assign a staff name; these visual concepts do not require forced names or cultural identity rules. Replace the producer pair at existing paths; keep vision, steady and marketer art. The producer's compact proportions address the clearest mismatch identified in the audit.
6. Replace only the current working-station foreground drawing with the appropriate transparent artwork. OfficeScene currently assembles a desktop and monitor with CSS spans. Use standard by default, animation for visual work and audio for sound if the existing assignment can support it. Preserve position, depth, movement, bubbles, click targets and energy UI. Check scaling against seated/standing workers and background furniture in all office tiers; the workstation PNG includes a tabletop and may need visual positioning to avoid doubling an existing desk.
7. ChatGPT/Arena should test roster loading, hidden-affinity concealment, pair discovery, all showrunner screens, worker feet/head scale, desk layering, low-resolution portrait readability and APK memory/load behaviour. Engine placement, mobile performance and the missing screenshot comparison have not been tested in this handoff.

Scoring formulas, review expectations, Web-series economy, format gating, commissions, studio bubbles, staff development, save migration and other mechanics are outside these changes. No mechanics should be replaced to import these files.

name: Build Staff IP Business Playtest APK

on:
  push:
    branches:
      - work/staff-potential-ip-business-pass
    paths:
      - .github/workflows/build-staff-ip-playtest-apk.yml

permissions:
  contents: write

jobs:
  build-apk:
    runs-on: ubuntu-latest
    timeout-minutes: 30

    steps:
      - name: Checkout locked playtest source
        uses: actions/checkout@v4
        with:
          ref: 98815b244322ff817f31bd35403bb4c5032b69a3
          fetch-depth: 0

      - name: Set up Node.js
        uses: actions/setup-node@v4
        with:
          node-version: 22
          cache: npm
          cache-dependency-path: game_source/package-lock.json

      - name: Set up Java
        uses: actions/setup-java@v4
        with:
          distribution: temurin
          java-version: "21"
          cache: gradle

      - name: Install dependencies
        working-directory: game_source
        run: npm ci

      - name: Run complete test suite
        working-directory: game_source
        run: npm test

      - name: Build web app and sync Capacitor
        working-directory: game_source
        run: npm run apk:sync

      - name: Make Gradle wrapper executable
        working-directory: game_source
        run: chmod +x android/gradlew

      - name: Build debug APK
        working-directory: game_source/android
        run: ./gradlew assembleDebug

      - name: Prepare named APK
        run: cp game_source/android/app/build/outputs/apk/debug/app-debug.apk Anime-Runner-Staff-IP-Business-Playtest.apk

      - name: Upload workflow artifact
        uses: actions/upload-artifact@v4
        with:
          name: anime-runner-staff-ip-business-playtest-apk
          path: Anime-Runner-Staff-IP-Business-Playtest.apk
          if-no-files-found: error
          retention-days: 14

      - name: Publish public GitHub release APK
        env:
          GH_TOKEN: ${{ github.token }}
        run: |
          TAG='staff-ip-business-playtest-98815b2'
          APK='Anime-Runner-Staff-IP-Business-Playtest.apk'
          gh release delete "$TAG" --cleanup-tag -y || true
          gh release create "$TAG" \
            "$APK" \
            --target 98815b244322ff817f31bd35403bb4c5032b69a3 \
            --title 'Anime Runner Staff/IP/Business Playtest' \
            --notes 'Android debug APK built from locked playtest commit 98815b244322ff817f31bd35403bb4c5032b69a3 on work/staff-potential-ip-business-pass. Full automated test suite is run before packaging.'

name: QoL playtest patch

on:
  push:
    branches:
      - work/qol-playtest-sept16

permissions:
  contents: write

jobs:
  apply:
    if: github.actor != 'github-actions[bot]'
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
        with:
          ref: work/qol-playtest-sept16
      - uses: actions/setup-node@v4
        with:
          node-version: 22
          cache: npm
          cache-dependency-path: game_source/package-lock.json
      - name: Apply final source patches
        run: node game_source/scripts/apply-qol-finalize.mjs
      - name: Install dependencies
        working-directory: game_source
        run: npm ci
      - name: Run tests
        working-directory: game_source
        run: npm test
      - name: Production build
        working-directory: game_source
        run: npm run build
      - name: Commit validated source fixes
        run: |
          if git diff --quiet; then
            echo "No source changes needed"
            exit 0
          fi
          git config user.name "github-actions[bot]"
          git config user.email "41898282+github-actions[bot]@users.noreply.github.com"
          git add game_source/src
          git commit -m "Finish QoL playtest pass"
          git push

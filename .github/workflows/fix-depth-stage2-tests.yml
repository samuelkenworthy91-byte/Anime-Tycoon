name: Fix Depth Pass Stage 2 Tests

on:
  push:
    branches:
      - work/research-showrunner-auction-depth-pass
    paths:
      - .github/workflows/fix-depth-stage2-tests.yml

permissions:
  contents: write

jobs:
  fix-and-validate:
    runs-on: ubuntu-latest
    timeout-minutes: 30
    steps:
      - uses: actions/checkout@v4
        with:
          ref: work/research-showrunner-auction-depth-pass
          fetch-depth: 0
      - uses: actions/setup-node@v4
        with:
          node-version: 22
          cache: npm
          cache-dependency-path: game_source/package-lock.json
      - name: Fix assertions
        working-directory: game_source
        run: node scripts/fix-depth-stage2-tests.mjs
      - name: Commit fix
        run: |
          git config user.name 'github-actions[bot]'
          git config user.email '41898282+github-actions[bot]@users.noreply.github.com'
          git add game_source/src/engine/__tests__/careers.test.ts
          git commit -m 'Align career tests with grouped genre specialisations' || true
          git push origin HEAD:work/research-showrunner-auction-depth-pass
      - name: Install
        working-directory: game_source
        run: npm ci
      - name: Targeted tests
        working-directory: game_source
        run: npx vitest run src/engine/__tests__/depth-pass-stage1.test.ts src/engine/__tests__/careers.test.ts
      - name: Full tests
        working-directory: game_source
        run: npm test
      - name: Production build
        working-directory: game_source
        run: npm run build

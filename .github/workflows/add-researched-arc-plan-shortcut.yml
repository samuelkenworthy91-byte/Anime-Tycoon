name: Add Researched Arc Plan Shortcut

on:
  push:
    branches:
      - work/staff-potential-ip-business-pass
    paths:
      - .github/workflows/add-researched-arc-plan-shortcut.yml

permissions:
  contents: write

jobs:
  patch:
    runs-on: ubuntu-latest
    timeout-minutes: 20
    steps:
      - name: Checkout branch
        uses: actions/checkout@v4
        with:
          ref: work/staff-potential-ip-business-pass
          fetch-depth: 0

      - name: Set up Node.js
        uses: actions/setup-node@v4
        with:
          node-version: 22
          cache: npm
          cache-dependency-path: game_source/package-lock.json

      - name: Install dependencies
        working-directory: game_source
        run: npm ci

      - name: Apply arc quick-plan patch
        working-directory: game_source
        run: python3 scripts/patch_arc_quick_plan.py

      - name: Run full test suite
        working-directory: game_source
        run: npm test

      - name: Build production app
        working-directory: game_source
        run: npm run build

      - name: Commit validated feature
        run: |
          git config user.name "github-actions[bot]"
          git config user.email "41898282+github-actions[bot]@users.noreply.github.com"
          git rm .github/workflows/add-researched-arc-plan-shortcut.yml
          git rm game_source/scripts/patch_arc_quick_plan.py
          git add game_source/src/components/Create.tsx
          git commit -m "Add one-tap researched story plan"
          git push origin HEAD:work/staff-potential-ip-business-pass
